# Secrets Master Password (required for encrypted secrets)
# This is the ONE secret that must remain in .env - all others are encrypted
SECRETS_MASTER_PASSWORD="v-Nzk8aKC-WmS9Tj4lXtGSFjJUzpBwVEJeu4nTaEm2A"

# Database URL is loaded from encrypted secrets (secrets.encrypted.json)
# Do NOT put DATABASE_URL here - manage it via /admin/database-config

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="temple-platform-secret-key-change-in-production-2025"

# Environment
NODE_ENV="development"

# Email Service (Phase F3)
EMAIL_PROVIDER="mock"
# EMAIL_API_KEY=""  # Set to your Resend or SendGrid API key in production
EMAIL_FROM="noreply@asembli.example.com"
EMAIL_FROM_NAME="Asembli Platform"
PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1

# Imgbb Image Hosting
IMGBB_API_KEY="009d3c14ad9a0611acd684e0109ab9f4"

# NOTE: The lines below were previously commands embedded in this file.
# They are intentionally removed. For quick local testing you can
# run the following in your shell (in the project root):
#
# PowerShell (temporary, insecure):
# $env:NODE_TLS_REJECT_UNAUTHORIZED="0"; npm run dev
#
# cmd (temporary, insecure):
# set NODE_TLS_REJECT_UNAUTHORIZED=0 && npm run dev
#
# Safer alternative: set `NODE_EXTRA_CA_CERTS` to point to a PEM file
# with your trusted CA, e.g. in this file:
# NODE_EXTRA_CA_CERTS="C:\\certs\\company-ca.pem"