"use client";

import React from 'react';

// Minimal TenantHeader placeholder — restores a valid component while keeping
// the file small and safe. The original, richer header behavior can be
// restored later if you prefer.
export default function TenantHeader() {
  return (
    <div className="py-3">
      {/* Tenant header placeholder */}
    </div>
  );
}
