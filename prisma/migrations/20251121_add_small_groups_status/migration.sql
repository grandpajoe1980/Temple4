-- Migration: add_small_groups_status
-- NOTE: This migration was added by hand. Prefer running `npx prisma migrate dev --name add_small_groups_status` to create a proper migration.
-- The Prisma CLI will compute the correct SQL for your database provider.

/* Placeholder: run the Prisma migration flow to generate the proper SQL for your environment.
   For a quick local dev workflow (SQLite), run:

     npx prisma migrate dev --name add_small_groups_status

   This will update the SQLite dev.db file and create a proper SQL migration.
*/
